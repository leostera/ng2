'use strict';

/* jasmine specs for controller ${_s.camelize(name)} go here */

describe('controllers', function (){
  beforeEach(module('${_s.camelize(module)}'));

  it('should ....', inject(function () {
    //spec body
  }));

  it('should ....', inject(function () {
    //spec body
  }));
});